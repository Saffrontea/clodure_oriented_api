[package]
name = "application"
version = "0.1.0"
edition = "2021"

[dependencies]
domain = { path = "../domain" }
uuid = { version = "1.6.1", features = ["v4", "serde"] }
async-trait = "0.1.68"
tokio = { version = "1.35.1", features = ["full"] }
